version: '3'
services:

  python:
    build:
      context: .
    env_file:
      - .env
    command:
      - whereis taskiq
      - taskiq scheduler tkq:sched
      - taskiq worker tkq:broker --fs-discover
      - python app.py
